# Event taxonomy — keep in sync with settings.THEMES (L1) and the
# orchestrator's classification prompt (L2).
themes:
  chokepoint_hormuz:
    category: chokepoint_disruption
    severity_default: 5
  chokepoint_suez:
    category: chokepoint_disruption
    severity_default: 4
  armed_conflict_escalation:
    category: armed_conflict
    severity_default: 4
  market_crash:
    category: financial_shock
    severity_default: 5
  sanctions_exportcontrols:
    category: statecraft
    severity_default: 3
